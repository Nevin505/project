const Button = ({children}) => {
  return (
    
        <button className="px-4 py-2 flex justify-center items-center  border-2 border-border-primary-600 hover:border-primary-700">{children}</button>
  )
}

export default Button
